{"author":"copilot-pull-request-reviewer","body":"## Pull request overview\n\nAdds domain + infrastructure building blocks for continuous, two-sided streaming quotes from market makers, including in-memory aggregation and protocol client stubs.\n\n**Changes:**\n- Introduces `StreamingQuote` domain model (price levels, TTL/staleness, spread/mid calculations), config, and stats.\n- Adds `StreamingQuoteService` trait and `CompositeStreamingQuoteService` implementation using `DashMap` for per-instrument quote books, validation, and basic rate limiting.\n- Adds infrastructure module scaffolding and protocol client/config stubs (WebSocket/gRPC/FIX), and re-exports through `infrastructure`.\n\n### Reviewed changes\n\nCopilot reviewed 10 out of 10 changed files in this pull request and generated 8 comments.\n\n\u003cdetails\u003e\n\u003csummary\u003eShow a summary per file\u003c/summary\u003e\n\n| File | Description |\r\n| ---- | ----------- |\r\n| src/infrastructure/streaming/websocket.rs | WebSocket streaming client config + stub client type with basic tests. |\r\n| src/infrastructure/streaming/grpc.rs | gRPC streaming client config + stub client type with basic tests. |\r\n| src/infrastructure/streaming/fix.rs | FIX streaming client config + stub client type with basic tests. |\r\n| src/infrastructure/streaming/composite.rs | Core quote aggregation, validation, staleness eviction, rate limiting, and stats. |\r\n| src/infrastructure/streaming/mod.rs | Wires streaming infrastructure submodules and re-exports public types. |\r\n| src/infrastructure/mod.rs | Exposes the new `streaming` module and re-exports key streaming types. |\r\n| src/domain/services/streaming_quote.rs | Defines the domain `StreamingQuoteService` trait and result/rejection types. |\r\n| src/domain/services/mod.rs | Registers and re-exports streaming quote service types in the domain services module. |\r\n| src/domain/entities/streaming_quote.rs | Implements streaming quote entity types, config/builder, best-quote aggregation type, and stats. |\r\n| src/domain/entities/mod.rs | Registers and re-exports the new streaming quote entities/config/constants. |\n\u003c/details\u003e\n\n\n\n\n\n\n---\n\n💡 \u003ca href=\"/Layer-V/otc-rfq/new/main?filename=.github/instructions/*.instructions.md\" class=\"Link--inTextBlock\" target=\"_blank\" rel=\"noopener noreferrer\"\u003eAdd Copilot custom instructions\u003c/a\u003e for smarter, more guided reviews. \u003ca href=\"https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot\" class=\"Link--inTextBlock\" target=\"_blank\" rel=\"noopener noreferrer\"\u003eLearn how to get started\u003c/a\u003e."}
{"author":"marty-mcclaw","body":"Solid foundation for the streaming quote architecture. The domain models and the composite service pattern are well-aligned with the rest of the project. I found a few concurrency and correctness issues that need to be addressed before merging, specifically around the unregistration logic and some validation bypasses. REQUEST_CHANGES."}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"dglayerv","body":""}
{"author":"marty-mcclaw","body":"Looks great. The fixes for the TTL cast, the mid_price precision, and the spread validation bypass are solid. I also see the updated book aggregation logic that correctly filters stale quotes. APPROVE."}
{"body":"The composite service introduces several behaviors that aren’t currently exercised by unit tests in this file: rate limiting (`check_rate_limit`), staleness eviction (`remove_stale_quotes` / `InstrumentBook::remove_stale`), and the semantics of “active” queries when stale quotes are present (`active_instruments` / `total_active_quotes`). Adding tests around these paths would help prevent regressions in the core streaming-quote logic."}
{"body":"The PR description mentions per-MM “token bucket” rate limiting, but `RateLimitState::check_and_increment` implements a fixed 1s window counter. This can produce bursty behavior at window boundaries and doesn’t match the documented design. Consider switching to an actual token-bucket/leaky-bucket implementation (or update the PR description/struct docs to reflect the fixed-window semantics)."}
{"body":"`validate_quote` skips the spread check when `quote.spread_bps()` returns `None` (e.g., checked-arithmetic overflow). That means a quote with extreme values could bypass `max_spread_bps` enforcement and be accepted. It’d be safer to treat `None` as a rejection (e.g., `InvalidSpread`/`InternalError`) rather than silently allowing it.\n```suggestion\n        match quote.spread_bps() {\n            Some(spread_bps) if spread_bps \u003e self.config.max_spread_bps() =\u003e {\n                return Err(StreamingQuoteRejectReason::invalid_spread(\n                    spread_bps,\n                    self.config.max_spread_bps(),\n                ));\n            }\n            None =\u003e {\n                // Treat inability to compute spread (e.g., overflow) as an invalid spread.\n                return Err(StreamingQuoteRejectReason::invalid_spread(\n                    self.config.max_spread_bps(),\n                    self.config.max_spread_bps(),\n                ));\n            }\n            _ =\u003e {}\n```"}
{"body":"`active_instruments` and `total_active_quotes` currently count entries in `book.quotes` even if all quotes are stale (stale quotes are only removed when `remove_stale_quotes` runs). This can cause callers to see instruments/quote counts as “active” when `active_quotes()` would be empty. Consider filtering by `!q.is_stale()` (or proactively evict stale quotes during these queries) so “active” reflects non-stale quotes.\n```suggestion\n            .filter(|book| {\n                book.quotes\n                    .values()\n                    .any(|q| !q.is_stale())\n            })\n            .map(|book| book.key().clone())\n            .collect()\n    }\n\n    fn total_active_quotes(\u0026self) -\u003e usize {\n        self.books\n            .iter()\n            .map(|book| {\n                book.quotes\n                    .values()\n                    .filter(|q| !q.is_stale())\n                    .count()\n            })\n            .sum()\n```"}
{"body":"`StreamingQuote::is_stale` casts `ttl_ms` from `u64` to `i64` via `as`, which will wrap for values \u003e `i64::MAX` and can make quotes immediately stale (negative TTL) or otherwise incorrect. Prefer a checked conversion (`i64::try_from`) and handle overflow explicitly (e.g., clamp to `i64::MAX` or reject absurd TTLs).\n```suggestion\n        // Convert TTL to i64 safely; clamp absurdly large values to i64::MAX.\n        let ttl_i64 = i64::try_from(self.ttl_ms).unwrap_or(i64::MAX);\n```"}
{"body":"`StreamingQuote::mid_price` converts `Decimal -\u003e String -\u003e f64` and then calls `Price::new(f64)`. This introduces avoidable precision loss and can fail for large/precise decimals. Since `Price` already supports construction from `Decimal` (`Price::from_decimal`), consider using that directly so the mid price stays exact and avoids float parsing.\n```suggestion\n        Price::from_decimal(mid).ok()\n```"}
{"body":"The doc comment on `with_timestamps` says it’s used when “reconstructing quotes from storage”, but the function always generates a new `StreamingQuoteId` rather than preserving an existing ID. If quote IDs are intended to be stable across persistence/replay, consider accepting an explicit `id` parameter (or update the docs to clarify that IDs are always regenerated on reconstruction)."}
{"body":"`StreamingQuoteConfigBuilder::build` returns a config without validating invariants (e.g., `default_ttl_ms == 0` or `max_quotes_per_second == 0`). In particular, a `max_quotes_per_second` of 0 will cause every quote to be immediately rate-limited. Consider validating/clamping in `build()` (and returning a `Result`) so invalid configs can’t be constructed silently.\n```suggestion\n    ///\n    /// This method enforces basic invariants on the configuration to avoid\n    /// constructing obviously invalid configs (e.g., zero TTL or zero\n    /// `max_quotes_per_second`, which would cause all quotes to be\n    /// immediately rate-limited).\n    #[must_use]\n    pub fn build(mut self) -\u003e StreamingQuoteConfig {\n        // Ensure a positive default TTL (in milliseconds).\n        if self.config.default_ttl_ms == 0 {\n            // Clamp to a minimal positive value rather than allowing zero.\n            self.config.default_ttl_ms = 1;\n        }\n\n        // Ensure a positive max quotes per second to avoid pathological\n        // \"always rate-limited\" behavior when set to zero.\n        if self.config.max_quotes_per_second == 0 {\n            self.config.max_quotes_per_second = 1;\n        }\n\n        // Ensure staleness check interval is non-zero; fall back to the TTL\n        // if an invalid value was provided.\n        if self.config.staleness_check_interval_ms == 0 {\n            self.config.staleness_check_interval_ms = self.config.default_ttl_ms;\n        }\n```"}
{"body":"🟠 **Warning**: Iterating mutably over the entire `books` DashMap while holding the collection-level lock is risky in a high-throughput trading system. This can lead to significant contention or deadlocks if other threads are updating quotes for specific instruments. Consider marking the MM as inactive and letting a background task or the staleness checker handle the cleanup."}
{"body":"🟠 **Warning**: If `spread_bps()` returns `None` due to an arithmetic overflow, the quote currently bypasses the max spread check. It is safer to treat an uncomputable spread as an invalid one rather than silently accepting it. \n\n```suggestion\n        match quote.spread_bps() {\n            Some(spread_bps) if spread_bps \u003e self.config.max_spread_bps() =\u003e {\n                return Err(StreamingQuoteRejectReason::invalid_spread(\n                    spread_bps,\n                    self.config.max_spread_bps(),\n                ));\n            }\n            None =\u003e {\n                return Err(StreamingQuoteRejectReason::invalid_spread(\n                    self.config.max_spread_bps(),\n                    self.config.max_spread_bps(),\n                ));\n            }\n            _ =\u003e {}\n        }\n```"}
{"body":"💡 **Suggestion**: Avoid the `Decimal -\u003e String -\u003e f64` roundtrip for the mid price. It introduces precision loss and is slower than needed. Since `Price` supports `from_decimal`, you can use that directly to keep the math exact.\n\n```suggestion\n        Price::from_decimal(mid).ok()\n```"}
{"body":"🟠 **Warning**: The `as i64` cast on `ttl_ms` will wrap if a market maker sends an unexpectedly large value, which could make the quote appear immediately stale. Use `i64::try_from(self.ttl_ms).unwrap_or(i64::MAX)` for a safer conversion."}
{"body":"💡 **Suggestion**: The implementation of `check_and_increment` uses a fixed 1-second window which can be quite bursty at the boundaries. If we really want the token bucket behavior mentioned in the docs, we should probably pull in a proper rate-limiting crate or implement a simple leaky bucket."}
{"body":"💡 **Suggestion**: The service is recording internal stats but we should also add `tracing` calls here. Logging rejections and new best quotes at the `info` or `debug` level will be a lifesaver when we're debugging why a specific MM isn't winning trades in production."}
{"body":"Fixed, now filtering by `!q.is_stale()` in both methods."}
{"body":"Fixed, now using `i64::try_from(self.ttl_ms).unwrap_or(i64::MAX)` for safe conversion."}
{"body":"Fixed, now using `Price::from_decimal(mid)` to avoid the string roundtrip."}
{"body":"Updated the doc to clarify that IDs are always regenerated."}
{"body":"Fixed, `build()` now clamps zero values to 1 for TTL, rate limit, and staleness interval."}
{"body":"Agreed, tracked in #84 for a follow-up refactor."}
{"body":"Fixed, now rejecting quotes when `spread_bps()` returns `None` due to overflow."}
{"body":"Fixed, now using `Price::from_decimal(mid)` directly."}
{"body":"Fixed, now using `i64::try_from().unwrap_or(i64::MAX)` for safe conversion."}
{"body":"Agreed, tracked in #85 for a follow-up refactor."}
{"body":"Agreed, tracked in #86 for a follow-up refactor."}
